import { LeadDetailSkeleton } from '@/shared/ui/console-skeletons';

/** The target detail page has its own shape — a title plus stacked form cards. */
export default function LeadDetailLoading() {
  return <LeadDetailSkeleton />;
}
